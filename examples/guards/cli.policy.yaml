# Example Caprail CLI guard policy.
# The package family is Caprail, while the compatibility config surface still
# uses the spec's current cliguard file/env naming.

settings:
  audit_log: none
  audit_format: jsonl

tools:
  gh:
    binary: gh
    description: GitHub CLI (read-only PR and issue access)
    allow:
      - pr list
      - pr view
      - pr diff
      - pr checks
      - issue list
      - issue view
      - repo view
    deny_flags:
      - --web

  gws:
    binary: gws
    description: Google Workspace CLI (read + draft only)
    allow:
      - gmail messages list
      - gmail messages get
      - gmail drafts create
      - gmail drafts list
      - gmail drafts get

  az:
    binary: az
    description: Azure CLI (read-only resource access)
    allow:
      - group list
      - group show
      - resource list
      - resource show
      - vm list
      - vm show
